# GDN 原理分析 — 从 Softmax 注意力到门控 Delta 网络

这份文档讲 GDN（Gated Delta Net）在算什么、为什么这么设计，不需要任何代码背景，文中也不出现函数名。想知道 vllm-ascend 怎么实现它，看配套的 `GDN_Implementation.md`。

三张配图在 `gdn_diagrams/` 下：整体流程 `gdn_concept_overview.png`、核心一步的循环 `gdn_concept_rw_cycle.png`、两种执行方式 `gdn_concept_parallel.png`。

## 符号表

全文就这几个符号，卡住的时候先回这张表对一对，看卡的是符号还是逻辑：

| 符号 | 名字 | 一句话 |
|---|---|---|
| q | 查询 | 这个 token 想查什么 |
| k | 标签 | 这个 token 关于什么话题 |
| v | 内容 | 这个 token 要记的新内容 |
| S | 记忆矩阵 | 已经记住的东西，一整张"话题→内容"的表 |
| γ | 遗忘门 | 旧记忆的保留比例，0 到 1 之间 |
| β | 写入门 | 这次的改写力度，0 到 1 之间 |
| g | 衰减 | 遗忘门的 log 值，γ = exp(g)，永远 ≤ 0 |

## 1. Softmax 注意力的两个麻烦

先把注意力本身说清楚。每个词想更新自己的表示，都要去"查阅"其他词，查阅用三样东西：q 是"我想查什么"，k 是每个词身上的标签，v 是每个词实际携带的内容。注意力就是拿我的 q 碰每个人的 k，越像的人，他的 v 我抄得越多，最后加权混合成我的新表示。

Softmax 注意力的做法像开全员大会。以三个词 A、B、C 为例，A 要更新自己：

```
打分：  A 的 q 分别碰每个人的 k          → 2.0,  5.0,  1.0
放大：  每个分数过 exp                   → 7.4,  148,  2.7   （差距被急剧拉开）
归一：  除以总和，变成百分比             → A 5%   B 94%   C 2%
混合：  A 的新表示 = 5%·v_A + 94%·v_B + 2%·v_C
```

exp 那一步是"聚焦"能力的来源：B 的原始分只比 A 高 2.5 倍，exp 之后变成 20 倍，轻微的偏好被放大成压倒性的关注。

这套机制有两个麻烦。

第一，注意力矩阵是 T×T 的大表。三个词只有九个分数，十万 token 的长文就是一百亿个。计算量 O(T²)，长上下文昂贵的根源在这。

第二，更致命的是归一化的分母依赖所有人。"B 占 94%"这个数，是 A 看了全部 k 之后才算出来的，谁也没法提前算好自己值多少。所以这个方法拆不开，也变不成递推：每生成一个新词，都要回头把历史所有词重新打一遍分。这就是为什么 Softmax 注意力必须存下完整的 KV Cache——所有历史词的 k 和 v 的清单——而且它随序列长度线性膨胀。

## 2. 线性注意力：换成记笔记

线性注意力的想法是：别开会了，每个词把自己知道的东西写进一本公共笔记本，谁来查就翻笔记本。

具体做法分两步。写入：每个词把"自己的标签乘自己的内容"写进笔记本，S = k₁v₁ᵀ + k₂v₂ᵀ + …。这本笔记的大小只由向量维度决定（比如 128×128），写一百个词是这么大，写十万个词还是这么大。查询：新词拿 q 去翻笔记本，o = q·S，一次矩阵乘完事。

### 2.1 为什么 exp 拆不开、别的拆得开

"能拆开"的准确含义是：打分函数能不能写成"只含 q 的向量"乘"只含 k 的向量"。只要长这样，q 就能从求和号里提出来，历史被压缩成一个和 q 无关的矩阵 S，可以被任何后来的词复用。

拿数字验证一下。设 q = [1, 2]，两个历史词 k₁=[2,0]、k₂=[0,1]，内容是 10 和 20。

老实逐个打分：score(q,k₁) = 2，score(q,k₂) = 2，输出 = 2×10 + 2×20 = 60。

先攒笔记本再查：S = k₁·10 + k₂·20，是一个 2×2 的表 [[20],[20]]，输出 = q·S = 1×20 + 2×20 = 60。一样。

两种算法殊途同归不是巧合，是点积的结构保证的：q 可以提到求和号外面，剩下的 S 只依赖历史。

那 exp 为什么不行？Softmax 的分子是 Σ exp(q·k)·v，exp 罩在 q·k 这个"乘积的和"外面。指数运算只有一个拆分法则，exp(a+b) = exp(a)·exp(b)，它只对"和"成立，对"积"没有任何办法。q 和 k 的分量在里面纠缠成一个数，拆不成"q 的一部分乘 k 的一部分"，于是 q 提不出去，每个 q 只能和每个 k 单独碰一次，T×T 的大表就是这么来的。

线性注意力干脆把 exp 换成一个拆得开的变换 φ（经典选择是 elu(x)+1，逐元素、输出恒正），打分变成 φ(q)·φ(k)，又回到"分项相乘再求加"的可拆结构。分母同理可拆，也是攒一个逐词累加的向量。

还有一个漂亮的视角：严格说 exp(q·k) 也能写成内积，但对应的变换是无穷维的（核函数理论）。所以 Softmax 注意力等于用无穷维 φ 的线性注意力，表达力强是因为"状态"有无穷容量；无穷维存不下来，只好存原始的 (k,v) 清单，这就是 KV Cache。线性注意力把 φ 换成低维的便宜版本，状态从无穷大缩到固定大小，代价是有损压缩。

### 2.2 S 里存了什么

用二维的小例子看。两个词写入：

```
词1："我的标签是动物类（k=[1,0]），内容是 [10, 20]"  → 写到表第 1 行
词2："我的标签是动作类（k=[0,1]），内容是 [30, 40]"  → 写到表第 2 行

S = [[10, 20],
     [30, 40]]
```

查询时，q=[1,0]（我关心动物类）翻表，一翻就翻到词 1 的内容；q=[0.8, 0.2] 得到按相似度比例混合的记忆。

所以 S 是这一层对"到目前为止整个上文"的记忆，形式是"看到什么样的关键词，就混合出什么内容"。注意这是有损压缩：真实模型里成千上万个词的 k 都挤在同一块空间里，必然互相覆盖、串味。这个压缩失真是后面所有改进的出发点。

顺带说命名：正式场合叫状态矩阵（state，RNN 传统），理解时不妨叫记忆矩阵，两个名字说的是同一个东西。本文统一叫记忆矩阵。

### 2.3 为什么必须更新它

线性注意力的运行方式决定了每个词到来时只发生两件事：读（拿 q 查 S 得到输出）和写（把自己也写进 S）。关键在写的因果：如果这个词不把自己写进 S，它对后面所有词来说就像从没存在过。没有 KV Cache 兜底，S 是唯一的记忆载体，更新它是为了未来，不是为了现在。

### 2.4 S 为什么是固定大小

S 是所有"标签乘内容"的累加，形状就是 k 和 v 的维度：每头一个 Dk×Dv 的表，典型 128×128。这两个数是模型设计时定的，写死在模型配置里。乘上头数就是单层的总量，跟序列长度无关——一千个词的上下文和一百万词的上下文，压进的是同一张表。

拿 Qwen3-Next-80B 感受一下：每序列每层 32 头 × 128×128 ≈ 1MB（bf16），十二个线性注意力层一共十几 MB，不管上下文多长。同模型的全注意力层，一百万上下文时 KV Cache 要以 GB 计。

## 3. GDN 的三个机制

纯加法写入的笔记本会变成垃圾场：信息越堆越多，数字越来越大，而且旧信息赖着不走——文章前面说"巴黎是法国首都"，后面说首都迁了，旧内容还留在表里，查询时新旧一起冒出来。

线性注意力家族的改进就是围绕"怎么管好这本笔记"展开的：

```
纯线性注意力：  S += k·v                     什么都留着
RetNet/Mamba：  S = γ·S + k·v                加了遗忘，旧笔记定期褪色
GDN：          S = γ·S + β·k·(v − 旧值)      写之前先查旧账，只写差额
```

GDN 每个词的完整动作是四步：

```
① 衰减：  S' = γ·S            旧记忆整体乘个 0~1 的系数
② 检索：  r = k·S'            "这个话题上，旧记忆里记的是什么？"
③ 写入：  S ← S' + β·k·(v − r)  只写新内容和旧记忆的差额
④ 读出：  o = (1/√Dk)·q·S     从刚更新完的记忆里取答案
```

遗忘门 γ 解决"旧信息无限堆积"：γ 趋近 1 是长期记忆，趋近 0 是快速翻篇。写入门 β 控制改写力度。Delta 规则是最有意思的一步：写入量是新内容减去检索到的旧值，如果旧记忆已经"答对"了（r 接近 v），几乎不写；差得越远，改得越狠。这是从"叠加"到近似"覆盖"的关键，也是最接近 Softmax 注意力精确更新能力的一步。

除以 √Dk 是从 Softmax 注意力继承的温度系数：点积的幅度天然随维度增长，除掉这个"虚胖"，分数大小只反映相关性。另外 q 和 k 都会先做归一化（除以自身长度），保证数值稳定。

门控原料 a、b 来自输入投影（下一节），具体计算是 g = −exp(A)·softplus(a+c)，β = sigmoid(b)，A 和 c 是训练学出来的参数，每个头一份。g 存 log 空间有个好处：连续多个词的衰减可以直接把 g 相加（乘变加），后面 prefill 的分块算法全靠这一条。

## 4. 整体流程

![GDN 整体计算流程（原理视角）](gdn_diagrams/gdn_concept_overview.png)

### 输入是什么

严格说，GDN 层每一步的输入是"一个新输入加两个随身状态"：x 是本批词的隐藏向量（新输入，每批换新）；S 是记忆矩阵（每层每序列各一本，跨步持久）；还有一个卷积滑窗（存本批之前紧邻几个位置的输入，给卷积续接用）。

x 是什么？每个词被"词嵌入加上前面所有层"加工出来的当前向量，此刻还是一锅没分工的混合信息，q、k、v 的区分都还没有。

两个状态不随 batch 传入，存在状态池里（每层每序列一个槽位），按槽位取出、算完写回。新序列第一次进来时 S 是零，只有续算和生成阶段才有非零的旧值。

### 五步流程

1. 线性投影（分工）：用训练学出的权重把 x 拆成六个角色——q 查询、k 标签、v 内容、a 和 b 两个门的原料、z 输出门。和标准 Transformer 的 QKV 投影是同一个套路，只是角色多一点。
2. 短窗因果卷积：q、k、v 各自和紧邻的前三个位置加权混合。线性注意力本身没有位置概念，卷积补上局部性。
3. 算两个门：由 a、b 算出每个词每个头的 γ 和 β。
4. Delta 规则核心：对记忆矩阵做"读-改-写"，下一节展开。
5. 输出调制：核心输出和 z 逐元素相乘（输出门控），归一化，最后一次投影。

## 5. 核心一步：读-改-写循环

![GDN 核心：记忆矩阵的读-改-写循环](gdn_diagrams/gdn_concept_rw_cycle.png)

生成阶段每个词绕这个圈一圈，就是 §3 的四步。两个值得体会的细节：读的是刚写完的 S，当前词的输出里已经包含自己刚写入的信息；写入量是"新内容减旧预测"，这是 Delta 规则的全部精髓。

每步计算量恒定，跟上下文长度无关，这是 GDN 生成阶段快、显存不涨的根源。

## 6. 两种执行方式和状态交接

![GDN 两种执行方式：递推与分块并行](gdn_diagrams/gdn_concept_parallel.png)

同一个递推，按场景选两种执行方式。

生成阶段每步只有一个词，老实绕圈就行，每步只有几次小矩阵乘。处理整段 prompt 时逐词递推就太慢了，但"衰减加 Delta 写入"没法直接并行——检索要用到之前所有步的结果，不像纯加法那样满足结合律。解法是把整段输入切成 64 个一组的块，块内用一种代数变换（WY 变换）证明 64 步串行更新等价于几次大矩阵乘，一次并行算完；块间串行，后一块用前一块的终态当初态。结果整段 prompt 的计算量是 O(T)，块内形状规整，对矩阵乘硬件非常友好。

最后是状态交接：prefill 算完整段，产出的最终记忆存进状态池，decode 每步从池里取回接着用。池里每条序列的状态大小恒定，不随上下文涨——KV Cache 的两个麻烦（§1）在这里同时解决。

## 7. 总结

| | Softmax 注意力 | 纯线性注意力 | GDN |
|---|---|---|---|
| 比喻 | 全员大会，两两对视打分 | 公共笔记本，纯叠加写入 | 笔记本加遗忘加查旧账改写 |
| prefill 计算量 | O(T²) | O(T) | O(T)，分块并行 |
| 生成一步 | O(T)，扫全部历史 | O(1) | O(1) |
| 存储 | KV Cache，线性膨胀 | 固定大小 S | 固定大小 S，状态池管理 |
| 更新旧信息 | 精确 | 只能叠加 | 近似覆盖（Delta 规则） |
| 主要短板 | 长、贵 | 记忆串味 | 递推不可直接并行，用分块补 |

整个设计是一条因果链：换掉打分函数，历史才能压进固定大小的记忆矩阵，换来 O(1) 推理和恒定显存，但压缩有损；用遗忘和差额改写把损失补回来，但递推变成串行；再用分块算法把并行性补回来。每一步都在解决上一步引入的问题。

---

## 附：原理到代码的对应

读完想落地到 vllm-ascend，概念和代码的对照见 `GDN_Implementation.md` 的出场人物表，这里给个速查：记忆矩阵就是状态池里的 ssm_state；读-改-写一圈是那个逐 token 递推算子；分块并行是六步流水线；两个门的计算是个 Triton 核；状态交接由状态池的槽位机制完成。
