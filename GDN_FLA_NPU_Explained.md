# fla-npu GDN 算子说明文档

> 本文说明 `flash-linear-attention-npu` 仓中 GDN（Gated Delta Net）算子的原理与实现，面向有一定并行计算基础、但不了解该仓代码的读者。配套图在 `vllm-ascend/gdn_diagrams/` 下：整体流程 `cmp_fla_npu.png`、与另两版对比 `cmp_three_side_by_side.png`。

---

## 1. GDN 是什么

GDN（Gated Delta Net）是一种线性注意力层，用于 Qwen3-Next 等混合架构模型。每个注意力头维护一个固定大小的**记忆矩阵 S**（128×128），逐 token 更新：

```
门控：  γ_t = exp(g_t) ≤ 1          遗忘门（旧记忆保留比例）
        β_t = sigmoid(b_t)          写入门（本次改写力度）
递推：  S_t = γ_t·S_{t-1} + β_t·k_t·(v_t − k_tᵀ·γ_t·S_{t-1})ᵀ
读出：  o_t = (1/√Dk)·q_tᵀ·S_t
```

大白话：**旧记忆先按 γ 褪色，再用标签 k 查一下"这个话题原来记了什么"，只写新内容 v 和旧值的差额（乘 β 控制力度），最后用查询 q 从记忆里取出答案**。

和 Softmax 注意力相比，它不用 KV Cache——记忆是固定大小的，生成阶段每步 O(1)，长上下文显存不膨胀。代价是递推天然串行，以及记忆压缩是有损的（GDN 用"遗忘 + 差额改写"把损失补回来）。

---

## 2. 核心难点：串行递推如何并行

递推公式里，第 i 个 token 的写入依赖"前面所有 token 写完后的记忆"——直接算是 T 步串行。GDN 的解法是**分块 + 结算**：

**切块**：序列按 64 个 token 一组切块，块间串行、块内想办法并行。

**块内的连环债**：块内每个 token 写入前要检索记忆，会"碰"到前面 token 已写的内容——它的真实写入 = 天真写入 − 该扣的部分，而"该扣的部分"本身也是别人修正过的。设第 i 个 token 的真实写入是 u_i：

```
u_i = β_i·v_i − Σ_{j<i} L[i,j]·u_j
L[i,j] = β_i·(k_i·k_j)·exp(gcum_i − gcum_j)
```

L 是"债务矩阵"：第 i 行 j 列记 token i 检索时会碰到多少 token j 已写入的内容（标签相似度 × 写入门 × 时间衰减）。64 个方程摞起来：

```
(I + L)·U = B        →        U = (I+L)⁻¹·B
```

(I+L)⁻¹ 是"结算矩阵"：把 64 个互相牵连的天真写入**一次换算成互不牵连的净写入**。求它有便宜解法——单位下三角用前代换（从第 1 行顺序解到第 64 行，每行只用已解的行），再配合分层合并（16×16 小逆 → Schur 补 → 64×64 大逆）。串行深度只有 15，且与序列长度无关。

**结算之后，块内 64 个写入互不干扰**，可以一股脑累加——递推变成矩阵乘。这就是整条流水线并行化的数学基础。

---

## 3. fla-npu 的实现结构：单核 Phase A-E

### 3.1 一次核启动

fla-npu 把上面整条计算链收进**一个 AscendC 算子**（`chunk_gdn_core_fwd`），一次核启动完成全部计算：

```
输入：q, k, v, β, raw_g（+可选 initial_state、变长序列元数据）
输出：o, final_state,（可选 g_cumsum、A）
```

核内按依赖关系分五个相位（Phase A-E），用跨核硬件旗标（SetFlag/WaitFlag）定顺序、用 GM workspace 传数据。

### 3.2 混合核编队：1 个 AI Core + 2 个 Vector 核

950 上该算子以 **MIX_AIC_1_2** 任务类型运行：1 个 AI Core（AIC，带矩阵乘单元）配 2 个 Vector 核（AIV）组成一组。核内代码按 `ASCEND_IS_AIC` / `ASCEND_IS_AIV` 分成两套分支——**矩阵乘拍在 AIC 上跑，逐元素拍在 AIV 上跑**，两套分支通过 GM workspace 传数据、通过跨核旗标互相同步。

这一架构有个内在代价：AIC 和 AIV 的片上缓存互相看不见，**每次"矩阵乘 → 逐元素"的接力都要经过 GM 中转**。fla-npu 的 flag 编排和流水设计，核心就是在压这笔中转税。

### 3.3 整体流程图

```
q, k, v, β, raw_g
       │
┌──────▼──────────────────────────────────────────────┐
│ Phase A（两类核并行起步）                            │
│   AIC：K·Kᵀ → score workspace                       │
│   AIV：g 块内 cumsum → g_cumsum workspace           │
└──────────────┬──────────────────────────────────────┘
        flag: score ready
┌──────────────▼──────────────────────────────────────┐
│ Phase B（AIV）                                       │
│   KKT 收尾：×β、×衰减、严格下三角 → 债务矩阵 L      │
│   SolveTri：求 (I+L)⁻¹（Cube+Vector 双实现）         │
└──────────────┬──────────────────────────────────────┘
        flag: solve done
┌──────────────▼──────────────────────────────────────┐
│ Phase C（核内 Dispatch）                             │
│   Recompute → w（净写入系数）、u（净写入内容）       │
└──────────────┬──────────────────────────────────────┘
        flag 复用
┌──────────────▼──────────────────────────────────────┐
│ Phase D（FwdH）：逐块状态递推，四拍编舞 × 每块       │
│   C1(Cube)：v_work = w·S_c        ← 算"回声"        │
│   V1(AIV)： v_update = u − v_work，×衰减            │
│   C2(Cube)：h_work = kᵀ·v_update  ← 64 写入批量加   │
│   V2(AIV)： S_{c+1} = 衰减·S_c + h_work             │
│             存快照 h、写 v_new；末块写 final_state   │
└──────────────┬──────────────────────────────────────┘
        SyncAll
┌──────────────▼──────────────────────────────────────┐
│ Phase E（FwdO）：逐块输出，五拍编舞 × 每块           │
│   C1(Cube)：attn = q·kᵀ                             │
│   V1(AIV)： 因果掩码 + 衰减 exp                      │
│   C2(Cube)：h_part = q·h_快照     ← 块间读出        │
│   C3(Cube)：v_part = attn·v_new   ← 块内注意力      │
│   V2(AIV)： o = scale·(h_part + v_part)             │
└──────────────┬──────────────────────────────────────┘
               ▼
        o · final_state（+ 可选 g_cumsum、A）
```

---

## 4. 各相位详解

### Phase A：相似度与衰减的并行起步

K·Kᵀ 只需要 k，cumsum 只需要 g——**零依赖**，所以 AIC 和 AIV 同时开算：

- **AIC 侧**：对每个 (块， 头) 计算 K·Kᵀ（64×128 · 128×64），结果进 score workspace；
- **AIV 侧**：对 g 做 64-token 块内前缀和（`chunk_local_cumsum`），得到 gcum——之后块内任意两点的累积衰减 = 查两个前缀和相减（γ(a→b) = exp(gcum[b] − gcum[a])）。g 全程 fp32，因为连乘的误差会被 exp 放大、累加不会。

两路都完成后置 score-ready 旗标汇合。

### Phase B：KKT 收尾 + 求逆

AIV 先做 KKT 的"收尾"（`ChunkScaledDotKktFusedCumsum` 的 epilogue）：给 K·Kᵀ 乘上 β 和衰减项 `exp(gcum_i − gcum_j)`、只保留严格下三角——得到**债务矩阵 L**。

然后 **SolveTri 求出 (I+L)⁻¹**。这一步是全流水线数学上最深的部分：

- 为什么要求逆：块内每个 token 的真实写入 u_i 依赖前面所有 token 的真实写入（修正之上还有修正），64 个方程摞成 (I+L)·U = B——解出 U 需要这个逆；
- 怎么算：单位下三角用前代换（串行深度 = 15，与序列长度无关），Cube 版和 Vector 版双实现按形状 dispatch，16/32/64 三种尺寸有各自特化；
- 产出的结算矩阵 (I+L)⁻¹ 存 aWorkspace，供 Phase C 使用。

### Phase C：WY 重构

Recompute 把结算矩阵用掉，产出两个量：

```
u = (I+L)⁻¹·(β·V)       净写入内容：每个 token 最终写进记忆的东西
w = (I+L)⁻¹·(βγ·K)      净写入里挂在"块初记忆 S₀"上的系数
```

为什么有两个：块内 token 检索时会碰到**块初记忆 S₀**（上一块传下来的），这部分被结算进 u 里了；但 S₀ 每块都不同，所以留一个 w·S₀ 的活口，Phase D 逐块推进时按当前真实状态扣掉。**到这一步，块内串行递推彻底消失——剩下的全是矩阵乘。**

### Phase D：跨块状态递推（四拍编舞）

从这里开始处理真正的串行：**后一块的状态依赖前一块的终态**。对每条序列、每个头，从第一块走到最后一块，每块四拍：

| 拍 | 单元 | 算什么 | 大白话 |
|---|---|---|---|
| C1 | Cube（AIC） | v_work = w·S_c | **算"回声"**：u 里有多少其实是块初记忆的回响 |
| V1 | AIV | v_update = (u − v_work) ⊙ 衰减 | **扣回声、按写入时间衰减** |
| C2 | Cube（AIC） | h_work = kᵀ·v_update | **64 个写入一次矩阵乘全加上** |
| V2 | AIV | S_{c+1} = 按行衰减·S_c + h_work | **合并出下一块的状态**，存快照 h、v_new |

两个细节：

- **状态驻留片上**：S_c 在 AIV 的 UB 里以 ping-pong 双缓冲跨块保留（96 KB 偏移处），只在序列开头搬入一次、结尾写出一次——中间几十个块零状态搬运；
- **按行衰减**：状态 S 的每一行是在不同时间写入的，衰减量按"写入时间到块尾"逐行不同（exp(g_块尾 − g_该行写入时刻)）——这就是 g_cumsum 除块内前缀和外还要存一份的原因。

每块的快照 h（状态在"块初时刻"的样子）和 v_new（修正后的值）存下来，是 Phase E 的两个输入。最后一块的终态写回状态池，decode 阶段从池里接续。

### Phase E：输出计算（五拍编舞）

每个 token 的输出是两项**相加**：

```
o_i = q_i·(衰减·S_块初)  +  Σ_{j≤i} exp(g_i−g_j)·(q_i·k_j)·u_j
      └─ 块间项 ─┘            └────── 块内项 ──────┘
```

- **块间项**：从"本块开始前的记忆"读出（查 Phase D 的快照 h，带衰减）——旧账；
- **块内项**：对本块的净写入 u 做因果注意力——本块新写的内容。减法全部发生在写入端（u = β(v − 检索值)），读出端看到的是净记忆，所以是加法。

| 拍 | 单元 | 算什么 |
|---|---|---|
| C1 | Cube | attn = q·kᵀ（块内两两相似度） |
| V1 | AIV | 因果掩码（挡住未来）＋ 衰减 exp |
| C2 | Cube | h_part = q·h_快照（块间项） |
| C3 | Cube | v_part = attn·v_new（块内项） |
| V2 | AIV | o = scale·(h_part + v_part) |

**注意这里的 attn 和 Phase B 的 L 不是同一张表**：B 的 L 是写侧的债务矩阵（严格下三角，只管扣账），E 的 attn 是读侧的注意力分数（含对角线——token i 读得到自己刚写入的这笔）。

---

## 5. 跨核协作机制：workspace 是传菜口，flag 是叫号器

混合核里 AIC 和 AIV 的片上缓存互相看不见，四拍/五拍之间的每次接力都走同一条路：

```
AIC 算完一拍 → 结果写 GM workspace → SetFlag（"菜好了"）
AIV 轮到它  → WaitFlag（等叫号）→ 从 GM 读进来 → 开算
```

- **workspace**：GM 里按偏移划分的中转区（v_work、v_update、kDecay、h_work 各有槽位，host tiling 时算好偏移）；
- **flag**：硬件事件（SetFlag/WaitFlag），带代数编号防陈旧触发；fla-npu 对 flag 的使用有精细的代数设计（哪些 flag 在哪些相位被置位/消费/复用，都有注释约束）；
- **ping-pong 双缓冲**：两路 stream 交替，AIC 算第 c+1 块时 AIV 收尾第 c 块，装载和计算重叠。

这是 NPU 混合核架构的代价（GPU 同一 CTA 内共享 SMEM，没有这笔税），也是为什么 ⑤⑥ 这类"多拍接力"的核必须手写 AscendC 而不是用 Triton。

---

## 6. H/O 块级流水（perf/gdn-a5-ho-generalized-pr 分支）

Phase D（FwdH）和 Phase E（FwdO）之间原本是全局屏障：**所有块**的快照算完才开始算输出。perf 分支改成块级流水：

- 28 个核分成 **16 个生产者组（跑 FwdH）+ 12 个消费者组（跑 FwdO）**，同时开工；
- FwdH 每算完一个 (块, 头)，用 **IBSet 硬件原子指令**在 GM 信号区置位（信号区复用 v_new 缓冲区之后的 workspace，32 字节一个槽）；
- FwdO 的消费任务用 **IBWait** 自旋等对应槽位——某块的信号一置位，它的 o 立即开始算，不用等所有块；
- 启用资格由 host tiling 下发（`HoPipelineContext`：enabled + 生产者/消费者组数），早期版本硬编码了 benchmark 场景的形状（如 seqlen==11274），后泛化。

效果：消掉"快照全落盘才开始读输出"的相位空泡——FwdH 还在推进后面的块时，FwdO 已经在消费前面的块。

---

## 7. 精度与数值设计

- **g 全程 fp32**（log 空间）：块内任意两点的累积衰减 = 查前缀和相减，一次 exp；若连乘 bf16，64 步的误差会被 exp 指数放大；
- **状态可选 fp32 / bf16**：递推是串行累加，误差不重算就留着——fp32 保精度，bf16 省搬运，按场景 dispatch；
- **算子内 exp 已走 exp2 路径**（× 1/ln2），匹配硬件快指令；
- 求逆用 fp32（下三角结构 + 前代换，数值稳定，无需选主元）。

---

## 8. 演进方向

| 方向 | 状态 |
|---|---|
| FwdH/FwdO 逐块交错（状态递推完一块就地算该块输出，免 h/v_new 落 workspace） | 未做——FlashQLA 在 GPU 上验证了此结构（双消费者 warpgroup），可借鉴 |
| Phase A-C 与 FwdH 的全链块级流水 | 未做——目前只有 D→E 流水；fwd_prepare 落地后接力点减少，实现难度降低 |
| workspace 的 L2 亲和布局 | 未做——跨核中转的热数据布局按核组局部性优化，视 profile 的 L2 命中率定量 |
| 门控驱动的 warm-up 序列切分 | 未做——FlashQLA 用衰减阈值判定"初态影响可忽略"的段，免修正并行 |
| PCP / 投机解码 | 未做（vllm-ascend 有） |

---

## 附：反向说明

fla-npu 同时包含 GDN 的反向套件（`chunk_gdn_bwd/` 下：`prepare_wy_repr_bwd` 系列准备反向 WY 结构、`chunk_bwd_dqkwg` 一次算 q/k/w/g 梯度、`chunk_bwd_dv_local` 局部 v 梯度、`chunk_gated_delta_rule_bwd_dhu` 状态梯度更新），以及 `causal_conv1d_bwd`——即前向+反向的完整训练支持，反向各核目前为独立算子（未做 Phase6 式融合）。
