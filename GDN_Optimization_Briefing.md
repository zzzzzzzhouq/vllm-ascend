# GDN 算子汇报讲稿（照读版）

> 使用说明（讲前看一眼，不用念）：全程约 20 分钟。三张图按文中提示打开：`gdn_dataflow.png`、`gdn_chunk_pipeline.png`（都在 gdn_diagrams 目录）。括号里的是动作提示，不用念。讲完正文后有一页问答预案，讲之前自己过一遍。

---

领导，今天想占用您二十来分钟，把 GDN 这个算子从头到尾讲一遍。它现在是我们模型 prefill 性能里的一个大头，我想请您帮忙看看还有哪些优化空间。我先把它是什么、怎么实现的、钱花在哪讲清楚，最后有几个候选方向想听您的判断。中间您随时打断。

**【一、GDN 是什么】**

先说这个算子是什么。GDN，全称 Gated Delta Net，是一种线性注意力层。在 Qwen3-Next 这类混合架构的模型里，它和标准的全注意力层是交错堆叠的——全注意力层负责精确检索，GDN 负责廉价地压缩长上下文。

它和全注意力最根本的区别在状态上。全注意力要存 KV Cache，序列多长缓存就多大；GDN 每个头只维护一个固定大小的记忆矩阵，128 乘 128，不管上下文是一千个 token 还是一百万个，它就这么大。生成的时候，全注意力每步都要扫一遍全部历史，GDN 每步只对这个固定矩阵做一次更新，天然就是 O(1)。

所以它是长上下文场景的成本命门，这正是值得专门优化的原因。

**【二、一条公式，三条路径】**

整个算子的数学核心就一条递推公式：

> S ← γ·S + β·k·(v − kᵀ·γ·S)，　o = (1/√Dk)·qᵀ·S

用话说就是：旧记忆先乘一个衰减系数 γ，这是遗忘门；用当前 token 的标签 k 去检索记忆里的旧值；写入的不是新内容 v 本身，而是 v 减掉检索值之后的差额，再乘一个写入门 β；最后拿查询 q 从更新后的记忆里读出输出。衰减、检索、写差额、读出，四个动作，每个 token 转一圈。

听起来很简单，麻烦在于：prefill 的时候是一整段 prompt，几千个 token 一个套一个，串行算太慢。所以这个算子实现上花的心思，全在"怎么把这条串行公式并行化"上。

（打开 gdn_dataflow.png）这张是整个算子的数据流图。前半段是三条路共用的：投影切出六个角色，过一次因果卷积，算出两个门，然后按 token 类型切分成三路。三路算的是**同一个公式**，只是执行方式不同：prefill 走六步分块流水线，这是默认路径；CANN 有个官方融合算子能一口气算完整段，但在我们 A5 环境上探测失败，基本是关着的；decode 和投机解码走右边那个 AscendC 手写的逐 token 递推。

所以今天优化的主战场很明确，就是中间这列六步流水线。decode 那条路每步就几个小矩阵乘，已经是延迟优化过的形态，暂时不是主要矛盾。

**【三、六步流水线】**

（打开 gdn_chunk_pipeline.png）这六步我过一遍，每步说清楚干什么、为什么。

第一步，把衰减的 log 值在每个 64-token 块内做前缀和。后面所有地方要的都是"两个位置之间累积衰减了多少"，而 exp 的可加性能把连乘变成累加——做完前缀和，任意两点间的衰减就是查两个数、相减、过一次 exp。g 全程用 fp32，因为连乘的误差会被 exp 放大，累加不会。

第二步，算一个 64 乘 64 的矩阵，我把它叫债务矩阵 L：第 i 行 j 列，记的是 token i 检索的时候能碰到多少 token j 已经写入的东西。两个 token 标签越像、隔得越近、后者写得越猛，这个数越大。它把块内"谁和谁的写入会撞车"这件事一次算清楚。

第三步，对 L 做下三角求逆，这是整个算法最核心也最绕的一步。每个 token 真正该写多少，要把和前面所有人的重复一次算清——数学上就是解一个方程组，(I+L) 乘 U 等于 B，本步算出解方程要用的 (I+L) 的逆。实现上先批量算 16×16 的小逆，再用 Schur 补拼成 64×64。

第四步，拿这个逆去乘两个"天真写入"，得到净写入 u 和系数 w。到这一步，块内 64 个写入就互不干扰了，可以一股脑累加——**串行递推在这里彻底变成了矩阵乘**。

第五步，块间推进状态。块内已经并行完，剩下的串行只在块和块之间——后一块要从前一块的终态接着算。这步是 AscendC 手写的，对每条序列、每个头，从第一块走到最后一块，顺手把每块的状态快照存下来给第六步用。

第六步，算输出。每个 token 的输出是两项相加：从本块开始前的记忆里读出来的部分，查第五步的快照；加上对本块净写入做注意力的部分。

这六步如果只能记一句：**前四步的全部意义，是把串行递推变成矩阵乘；第五步是残余的串行；第六步是纯读出。**

**【四、钱花在哪】**

接下来是最关键的账本。我把每个核的计算量和访存量按公式数了一遍，先说三个数：每个 token、每个头，计算量大约 12 MFLOP，访存量大约 7.9 KB。两个一除，算力强度 1.5 KFLOP 每字节——而机器的平衡点大概在 1 KFLOP 每字节。意思是：这条流水线正卡在计算和访存各占一半的平衡点上，两边都有优化空间，但任何单边的削减，只能兑现一半左右的时间。

再看这 7.9 KB 的访存花在哪，最大的三块：第一，第四步和第五步之间为了布局转换，五个张量各读写一遍显存，占四分之一还多——纯搬运，一点数学都不算；第二，二三四步之间，中间量 L、求逆结果依次落盘再读回，占六分之一；第三，第五六步拆成两个算子的代价，状态快照和修正值写下去再读回来，又占四分之一。

这里我必须诚实说明：这是纸面推算，按公式和核的实现数出来的，还没有跑过 profiling。所以我的第一个建议行动项，就是用 msprof 跑一次 8K prefill，把六个核各自的耗时、带宽利用率、Cube 利用率拿出来校准这张账——如果带宽利用率已经很高，流量类优化的收益取上限；如果 Cube 利用率低，就要先看算法类方向。

**【五、已经做过的两件事】**

在请您分析之前，先交代前辈已经落地的两处优化，就是图上标了星号的两处。

第一个，矩阵乘静态 tiling：第二步和第四步的矩阵乘，形状全部定成编译期常量，编译器把切块和流水一次定死，运行期零调度开销。代价是形状一变就触发重编译，后来专门有两个提交治理这个问题。

第二个，求逆里的标量往返消除：原来行和行之间的依赖，靠"标量写显存、两对 flag 定序"来实现，改成了预制一份单位阵、按行号直接读，从"写、等、读"变成纯读。

讲这两件是想说明这个仓吃什么样的优化——融合、访存、编译期调度这一类，而不是改数学。

**【六、约束和候选方向】**

然后是约束，有几样东西动不了。递推公式本身，那是模型权重定义的语义；状态池的接口，那是和 vLLM 调度器的契约；状态必须保 fp32，这是精度红线，递推的误差不重算就会一直留着；多卡修正依赖"状态更新对初态是线性的"这条性质，它是多卡正确性的根基；还有图回放安全，输出要预置零值、核心计算要包在 custom op 边界里。除了这些，剩下都是自由度：核怎么切、怎么融合、内存布局、求逆的算法、五六步怎么拆、launch 参数，甚至块大小本身——64 不是神圣数字，是片上缓存容量约束下的选择。

我自己按收益乘可行性列了六个候选方向，数字都是纸面估算，当讨论的引子：

- A，去掉四五步之间的布局转置，只改寻址不碰数学，预期端到端 8 到 15 个点，改动最小、收益最确定；
- B，把二三四步融合成一个核，中间量不落盘，预期 10 个点；
- C，求逆换成矩阵乘倍增算法，把最串行的一步变成纯矩阵乘，这一步本身可能快一倍，端到端 5 个点上下；
- D，五六步全融合、状态驻留片上，只剩输入输出的必要流量，流量能降三分之二，端到端 20 到 30 个点，但工程量最大；
- E，状态池进出各两次拷贝可以消掉，多短序列场景约 5 个点；
- F，几个小核合并，1 到 2 个点。

**【收尾】**

最后想请您帮忙判断三件事：一，这些方向里哪些值得先投；二，D 这种大融合在 NPU 上的工程风险您怎么看——第五六步现在拆开是有道理的，第五步块间串行、第六步块级完全并行，拆开让第六步吃得满核，合体就要拿并行度换访存，这个权衡值不值；三，有没有我们没想到的维度。

我这边接下来的动作：先跑 msprof 校准账本，再按今天定的优先级启动；CANN 新包发布、融合算子可用之后，我会第一时间做对比，到时候优先级可能要重排。以上就是全部内容，您看从哪里聊起。

---
---

## 附：问答预案（讲完正文被问到时用，讲前自己过一遍）

**Q：为什么块是 64？**
片上缓存（192KB）约束下的选择。放大块能减少块间串行次数，但单块工作集变大，是个可调的旋钮。

**Q：第五六步为什么不直接合成一个核？**
第五步块间串行，并行度只有序列数×头数；第六步块级完全并行。拆开让第六步吃满所有核。合体（方向 D）要拿并行度换访存，是 D 的核心权衡。

**Q：CANN 融合算子为什么不用？**
A5 的 CANN 包里没这个实现，直接调用报错；代码有探测机制，失败永久回退。等新包发布后值得第一时间对比。

**Q：什么数据量有代表性？**
prefill 段典型 8K；batch 构成影响第五步的并行度，profiling 要覆盖多短序列场景。

**Q：精度怎么兜底？**
状态 fp32、g 全程 fp32，只有 CANN 路径过一次 bf16；任何优化落地都跑 GSM8K 对齐。

**Q：decode 路径要不要一起看？**
它已经是延迟形态——槽位原地更新、无 host 同步，暂时不是主矛盾，但投机解码的状态回卷正确性约束在那边。

**Q：多卡怎么分工的？**
按序列切段，每卡一段，各算各的；初态依赖通过"两轮 all_gather + 本地代数修正"解决，状态更新对初态的线性性保证修正一次就够，不需要迭代。
