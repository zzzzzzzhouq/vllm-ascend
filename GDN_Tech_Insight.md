# 门控 Delta 网络（GDN）技术洞察：线性注意力如何找回表达力

> 本文面向对 Transformer 注意力有基本了解、想弄清线性注意力新进展的读者。不要求任何代码背景。Qwen3-Next 等新一代混合架构模型中，GDN 正在替代一部分全注意力层——这篇文章讲清楚它是什么、为什么有效，以及它的串行递推如何被改造成矩阵乘。

## 一、注意力的两种代价

Softmax 注意力的机制大家熟悉：每个 token 拿自己的查询 q 与所有历史 token 的键 k 打分，分数经过 exp 放大和归一化变成权重，再对所有历史 token 的内容 v 加权求和。它有两个绕不开的代价：

**代价一：T×T 的打分表。** 一万个 token 就要算一亿次两两相似度，prefill 的计算量随长度平方增长。

**代价二：KV Cache 随长度线性膨胀。** 归一化的分母依赖全部历史，谁也没法提前把自己"值多少注意力"算好，所以每个历史 token 的 k、v 都必须原样存着，随取随用。百万 token 上下文时，KV Cache 以 GB 计，而且生成阶段的每一步都要把整本缓存扫一遍。

线性注意力的目标就是同时消灭这两个代价。GDN（Gated Delta Net）是这条路上目前最有代表性的设计之一。

## 二、核心思路：把"档案柜"换成"笔记本"

线性注意力做的第一件事是换掉打分函数。Softmax 里的 exp 有一个致命性质：它作用在 q·k 这个"乘积的和"上，而指数运算只对"和"有拆分法则（exp(a+b)=exp(a)·exp(b)），对"积"无能为力——q 和 k 在里面纠缠成一个数，永远拆不成"q 的一部分乘 k 的一部分"。

线性注意力把 exp 换成一个可拆的变换 φ，打分变成 φ(q)·φ(k)。别小看这一步换元，它让整个历史可以**压缩**：

```
所有历史词的贡献 S = k₁v₁ᵀ + k₂v₂ᵀ + …（一个固定大小的矩阵）
任何一个查询的输出 o = q·S（一次矩阵乘）
```

S 的形状就是 k 和 v 的维度（每头一个 128×128），和序列长度无关。写入可以逐 token 进行，查询只需查表——T×T 的大表和线性膨胀的缓存同时消失。

用一个小例子看 S 存了什么。两个词写入：词 1 标签是"动物类"（k=[1,0]），内容 [10,20]；词 2 标签是"动作类"（k=[0,1]），内容 [30,40]。各自把"标签乘内容"写进表：

```
S = [[10, 20],      查询 q=[1,0]（我关心动物类）翻表：q·S = [10,20]
     [30, 40]]      ← 正是词 1 的内容。标签相似度决定读到谁。
```

所以 S 的本质是一张**"关键词 → 内容"的联想记忆表**：把整个上文有损压缩进一张固定大小的表，查询时按相似度混合取回。

## 三、有损的代价，GDN 的三个机制

压缩是有代价的。纯加法写入的笔记本是个垃圾场：信息无限堆叠，数字越滚越大；旧信息赖着不走——前面写过"巴黎是法国首都"，后面剧情改了，旧内容还留在表里和新区混在一起。

GDN 用三个机制对症下药。每个新 token 到来时：

```
① 衰减：  S' = γ·S                旧记忆整体乘一个 0~1 的系数（遗忘门 γ）
② 检索：  r = k·S'               "这个话题上，旧记忆记的是什么？"
③ 写入：  S ← S' + β·k·(v − r)    只写新内容和旧记忆的差额（写入门 β）
④ 读出：  o = (1/√Dk)·q·S         从刚更新完的记忆里取答案
```

**遗忘门 γ** 解决堆积：γ 趋近 1 是长期记忆，趋近 0 是快速翻篇，每个 token、每个头独立决定。**写入门 β** 控制改写力度。最有意思的是 **Delta 规则**——写入量是新内容减去检索到的旧值。如果旧记忆已经"答对"了（r 接近 v），几乎不写；差得越远，改得越狠。

用一个例子看 Delta 规则的"覆盖"能力：三个 token 谈同一个话题（标签相同、写入门为 1），它们各自写进记忆的量是 v₁、v₂−v₁、v₃−v₂——每个只写和前一任的差。三笔累加：v₁ + (v₂−v₁) + (v₃−v₂) = v₃，记忆里只剩最新值。**同话题完美覆盖**——这正是纯加性线性注意力做不到、Softmax 注意力靠归一化才能做到的事。GDN 用"先查旧账、再写差额"近似实现了它。

两个工程细节值得一提：衰减 γ 以 log 形式（g = log γ ≤ 0）存储，好处是连续多步的衰减可以**把乘变成加**——这颗种子后面会长成分块算法的关键一步；q、k 做归一化，配合 1/√Dk 的温度缩放，保证记忆的数值范围受控。

## 四、串行的诅咒

到这里一切美好，除了一个问题：**这个递推没法直接并行**。

纯加性线性注意力的写入满足结合律——任意分组先算部分和再汇总，结果一样，所以可以用并行扫描（parallel scan）把 T 步折成 log T 步。但 Delta 规则里，token i 的写入量依赖"检索到的旧值"，而旧值里包含 token j<i 写入的内容——**状态出现在自己更新方程的右边**。不满足结合律，并行扫描失效；老老实实逐 token 递推，prefill 一段几千 token 的 prompt 就要串行几千步，GPU/NPU 的并行算力全部闲置。

GDN 面对的矛盾非常清晰：**Delta 规则给了它接近 Softmax 的信息更新能力，代价是丢掉了并行性**。而分块算法（WY 表示）就是把并行性拿回来的钥匙。

## 五、分块算法：把 64 步串行变成矩阵乘

思路是把序列切成 64 个 token 一组的块，分两步走：**块内**，证明 64 步串行更新等价于几次大矩阵乘，一次并行算完；**块间**，后一块用前一块的终态当初态，保持串行。串行度从 T 步降到 T/64 步，而且剩下的部分粒度粗、正好交给访存友好的实现。

块内并行的关键在"解连环债"。块内每个 token 的净写入满足：

```
u_i = β_i·v_i − Σ_{j<i} L[i,j]·u_j
L[i,j] = β_i · (k_i·k_j) · (j 到 i 的累积衰减)
```

L 就是"债务矩阵"：第 i 行 j 列记录 token i 检索时会碰到多少 token j 已经写入的内容。麻烦在于 u_i 依赖 u_j，u_j 又依赖更早的——每个 token 写入前要扣前面所有人的账，而那些账本身也是扣过的。把 64 个方程摞起来是一个线性方程组：

```
(I + L)·U = B        →        U = (I+L)⁻¹·B
```

其中 B 是"天真写入"（不考虑互相干扰时各自想写的量）。(I+L)⁻¹ 就是把连环债务**一次算清的结算矩阵**。它有几个好性质：

- **I+L 是单位下三角**，求逆用前代换即可，便宜；
- **L 幂零**：严格下三角的 64×64 矩阵满足 L⁶⁴=0，所以 (I+L)⁻¹ = I − L + L² − … 的级数 64 项后必然终止（L² 是"债的债"——前面的修正反过来影响我该扣多少）；
- 结算矩阵算好之后，64 个净写入**互不干扰**，可以一股脑累加——递推从此变成矩阵乘。

用前面的同话题例子验证：L 是全 1 严格下三角时，(I+L)⁻¹ 恰好是一阶差分算子，天真写入 [v₁,v₂,v₃] 被结算成 [v₁, v₂−v₁, v₃−v₂]——和第三节的覆盖结论严丝合缝。

结算完成后，整个块的更新可以写成几次规整的矩阵乘：一个矩阵负责"每个 token 往记忆里写什么"，一个系数矩阵负责"扣除块初状态的影响"；输出则是两部分之和——从块开始前的记忆里读出的部分，加上对本块净写入做因果注意力的部分。减法全部发生在写入端的差额计算里，读出端看到的是干净的记忆。

## 六、两种执行方式，一次状态交接

同一个递推公式，按场景选执行方式：

**生成阶段（decode）**：每步只有一个 token，直接绕"衰减→检索→写差额→读出"一圈。每步只有几次小矩阵乘，计算量与上下文长度无关——O(1)，这是线性注意力的本命场景。

**预填充阶段（prefill）**：整段 prompt 用第五节的分块算法，块内并行、块间串行，计算量 O(T)。

两段之间靠**状态池**衔接：prefill 算完整段 prompt 得到的最终记忆，作为 decode 阶段的初始记忆。每条序列占一个固定大小的槽位（每头一个 128×128 的矩阵），不随上下文增长。KV Cache 的两个代价——平方计算量和线性膨胀的缓存——在这里同时被解决。

这也解释了混合架构的设计逻辑：Qwen3-Next 这类模型让全注意力层和 GDN 层交错工作——全注意力保留精确检索的能力（逐字引用第 800 个词这种事，压缩记忆做不好），GDN 承担廉价的长上下文压缩。各干各的擅长。

## 七、三点洞察

**1. 每一步优化都在解决上一步引入的问题。** 换掉打分函数，历史才能压缩进固定大小的状态——但压缩有损；遗忘门和 Delta 规则把信息更新能力补回来——但递推变成串行；WY 分块算法把并行性补回来——但块间仍有 T/64 步串行；多卡切分进一步摊薄——靠状态更新对初态的线性性保证修正只需一次。GDN 的设计史就是这条不断"补回来"的链。

**2. 硬件约束反向塑造算法。** 分块为什么是 64 不是 128 或 16？片上缓存的容量预算是直接原因。Delta 规则为什么"恰好"能用矩阵乘结算？因为它的更新对状态是仿射的。看起来是算法选择的地方，往往藏着硬件的形状。

**3. 表达力和效率的交换是有公允价的。** 线性注意力丢掉 exp 的"无穷维状态"，换来固定显存和 O(1) 生成；GDN 用遗忘和差额改写把更新精度补回大半。它不是 Softmax 的替代品，而是让"该便宜的地方便宜"的分工者。

---

## 附：公式速查

```
门控：     γ_t = exp(g_t)，g_t = −exp(A)·softplus(a_t+c) ≤ 0；  β_t = sigmoid(b_t)
递推：     S_t = γ_t·S_{t−1} + β_t·k_t·(v_t − k_tᵀ·γ_t·S_{t−1})ᵀ
读出：     o_t = (1/√Dk)·q_tᵀ·S_t
分块：     债务矩阵 L[i,j] = β_i·(k_i·k_j)·exp(gcum_i − gcum_j)，仅 i > j
结算：     (I + L)·U = B，U = (I+L)⁻¹·B（B 为天真写入）
幂零性：   L⁶⁴ = 0 ⟹ (I+L)⁻¹ = I − L + L² − … + (−1)⁶³L⁶³
```

*本文基于 vllm-ascend 项目对 Gated DeltaNet / flash-linear-attention 系列工作的实现与学习整理。*
